<Page
    x:Class="InwentarzRzeczowy.UWP.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InwentarzRzeczowy.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:appmodel="using:Windows.ApplicationModel"
    xmlns:reactiveUi="using:ReactiveUI"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <NavigationView x:Name="NavView" Header="{x:Bind appmodel:Package.Current.DisplayName}" ItemInvoked="NavView_OnItemInvoked">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Tag="Home" Content="Home" />
                <NavigationViewItem Icon="Message" Tag="Add" Content="Add" />
            </NavigationView.MenuItems>
            <reactiveUi:RoutedViewHost 
                Grid.Row="0" 
                x:Name="RoutedViewHost"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch" />
        </NavigationView>

        <Grid Background="Transparent" VerticalAlignment="Top" >

            <TextBlock Text="{x:Bind appmodel:Package.Current.DisplayName}" Grid.Column="1" Style="{StaticResource CaptionTextBlockStyle}" Margin="8,8,0,0"/>
        </Grid>

    </Grid>
</Page>
